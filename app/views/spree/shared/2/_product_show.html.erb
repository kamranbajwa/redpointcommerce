
        <section class="container margintop">
          <div class="row">
          <div class="custom-alert alert-success" style="display: none;"> Successfully Added to cart </div>
            <div class="col-md-9">

              <div class="row">
                <div class="col-sm-6 col-md-6">
                  <div id="product-images" data-hook="product_images">
                    <div id="main-image" class="line-hat" data-hook>
                      <div class="product-image-wrapper">
                        <%= render :partial => 'image' %>
                      </div>
                    </div>
                    <div id="thumbnails" data-hook>
                      <%= render :partial => 'thumbnails' %>
                    </div>
                    <div data-hook="product_properties" class="details-titles">
                      <%= render :partial => 'properties' %>
                    </div>
                    <div data-hook="promotions">
    
                      <%= render :partial => 'spree/products/promotions' %>
                    </div>
                  </div>
                </div>

                <div class="col-sm-6 col-md-6">
                  <input type="hidden" name="action" value="cart_add" />
                  <input type="hidden" name="product_id" value="1" />

                  <h2 class="product-title"><%= @product.name%></h2>

                  <span class="code">PART NUMBER: <%=@product.sku%></span>

                <div id="cart-form" data-hook="cart_form">
                  <%= render :partial => "spree/shared/#{@selected_template.template_no}/cart_form" %>
                </div>
                <div class="desc">
                  <h3 class="page-header">DESCRIPTION</h3>
                  <p>
                   <%= product_description(@product) rescue Spree.t(:product_has_no_description) %>
                 </p>
               </div>

               <div class="socialApis">
                <h3 class="page-header">TELL TO YOUR FRIENDS</h3>

                <div class="api">
                  

                 <div class="fb-like" data-href=<%=request.url%>  data-layout="standard" data-action="like" data-show-faces="true" data-share="true"></div>
               </div>
               <div class="api">
                <a href=<%=request.url%> class="twitter-share-button" data-lang="en">Tweet</a>
              </div>

              <div class="api">
                <div class="g-plusone" data-size="medium" data-annotation="inline" data-width="300"></div>
              </div>

            </div>

                </div>
              </div>
            
            </div>

            <aside class="col-md-3">
              <h3>CATEGORIES</h3>
              <ul class="nav nav-list">
                <% @product.taxons.each do |taxon| %>
                  <li><%= link_to taxon.name, seo_url(taxon) %></li>
                <% end %>
              </ul>
            </aside>
            <div class="col-md-12">
                <hr />
              <%= render :partial => "spree/shared/common/related_products" %>
            </div>
          </div>
        </section>
      </div>
    </div>
    <%= render :partial => "spree/shared/2/popup_modal" %>

    